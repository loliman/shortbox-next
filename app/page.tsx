export default function HomePage() {
  return (
    <main className="page">
      <section className="card">
        <p className="eyebrow">Shortbox</p>
        <h1>Next.js migration starts here.</h1>
        <p className="copy">
          The empty app shell is ready. We can port the frontend and GraphQL backend
          step by step from this baseline.
        </p>
      </section>
    </main>
  );
}
